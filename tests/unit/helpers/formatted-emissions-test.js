import {
  formattedEmissions
} from '../../../helpers/formatted-emissions';
import { module, test } from 'qunit';

module('FormattedEmissionsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formattedEmissions(42);
  assert.ok(result);
});
